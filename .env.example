# Serper.dev — used for search & research (find official site, contact info, competitors)
# Get a key at https://serper.dev
SERPER_API_KEY=your_serper_api_key_here

# OpenRouter — used for all AI analysis (summary, pain points, competitors)
# Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# NOTE: Discord Bot Token & Channel ID are NOT set here.
# They are entered at runtime on the /settings page (session-only,
# never persisted server-side), per the assignment's "no database" rule.
